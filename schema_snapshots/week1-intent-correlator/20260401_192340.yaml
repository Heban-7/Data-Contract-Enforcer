kind: DataContract
apiVersion: v3.0.0
id: week1-intent-correlator
info:
  title: Auto-generated contract for week1-intent-correlator
  version: 1.0.0
  owner: data-engineering-team
  description: Contract generated from outputs/week1/intent_records.jsonl. Covers
    9 fields.
servers:
  local:
    type: local
    path: outputs/week1/intent_records.jsonl
    format: jsonl
terms:
  usage: Internal inter-system data contract. Do not publish.
  limitations: confidence must remain in 0.0-1.0 float range.
schema:
  intent_id:
    type: string
    required: true
    format: uuid
    pattern: ^[0-9a-f-]{36}$
  description:
    type: string
    required: true
    enum:
    - Extract named entities from uploaded PDF documents
    - Route incoming events to appropriate handlers
    - Transform raw CSV data into normalized schema
    - Validate user authentication tokens before processing
    - Generate summary report from extracted facts
  governance_tags:
    type: string
    required: true
  created_at:
    type: string
    required: true
    format: date-time
  code_refs_file:
    type: string
    required: true
  code_refs_line_start:
    type: integer
    required: true
    minimum: 5.0
    maximum: 197.0
  code_refs_line_end:
    type: integer
    required: true
    minimum: 15.0
    maximum: 235.0
  code_refs_symbol:
    type: string
    required: true
  code_refs_confidence:
    type: number
    required: true
    minimum: 0.0
    maximum: 1.0
    description: Confidence score. Must remain 0.0-1.0 float. BREAKING if changed
      to 0-100.
quality:
  type: SodaChecks
  specification:
    checks:
    - missing_count(intent_id) = 0
    - duplicate_count(intent_id) = 0
    - missing_count(description) = 0
    - missing_count(governance_tags) = 0
    - missing_count(created_at) = 0
    - missing_count(code_refs_file) = 0
    - missing_count(code_refs_line_start) = 0
    - missing_count(code_refs_line_end) = 0
    - missing_count(code_refs_symbol) = 0
    - missing_count(code_refs_confidence) = 0
    - min(code_refs_confidence) >= 0.0
    - max(code_refs_confidence) <= 1.0
    - row_count >= 1
lineage:
  upstream: []
  downstream:
  - id: service::week3-refinery
    description: service::week3-refinery consumes data from table::extractions
    fields_consumed:
    - doc_id
    - extracted_facts
  - id: external::langsmith
    description: external::langsmith consumes data from pipeline::extraction-pipeline
    fields_consumed:
    - doc_id
    - extracted_facts
  - id: service::week5-event-platform
    description: service::week5-event-platform consumes data from table::events
    fields_consumed:
    - doc_id
    - extracted_facts
  - id: pipeline::event-pipeline
    description: pipeline::event-pipeline consumes data from service::week5-event-platform
    fields_consumed:
    - doc_id
    - extracted_facts
